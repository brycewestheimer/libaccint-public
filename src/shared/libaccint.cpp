// libaccint.cpp
// Core library compilation unit

#include "libaccint/config.hpp"

namespace libaccint {

// Core compilation unit — actual implementations are in dedicated source files.
// This file exists to ensure the library always has at least one translation unit.

} // namespace libaccint
